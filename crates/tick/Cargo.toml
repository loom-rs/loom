[package]
name = "tick"
version = "0.1.0"
edition = "2024"

[dependencies]
camino = "1.2.2"
tick_rt = { path = "../tick_rt" }
tick_common = { path = "../tick_common" }
miette = { git = "https://github.com/tick-io/miette", features = ["fancy"] }
clap = { version = "4.5.59", features = ["derive"] }
