[package]
name = "tick_sema"
version = "0.1.0"
edition = "2024"

[dependencies]
tick_ast = { path = "../tick_ast" }
miette = { git = "https://github.com/tick-io/miette", features = ["fancy"] }
thiserror = "2.0.18"
tick_common = { path = "../tick_common" }
