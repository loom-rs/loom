[package]
name = "geko-std"
version = "0.1.0"
edition = "2024"

[dependencies]
dune = { path = "../../dune"}
common = { path = "../../common"}
miette = { git = "https://github.com/geko-rs/miette", features = ["fancy"] }
